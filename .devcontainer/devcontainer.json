{
  "name": "qrender",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/qrender",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "tamasfe.even-better-toml"
      ]
    }
  },
  "forwardPorts": [
    4243
  ],
  "postCreateCommand": "apk add --no-cache musl-dev openssl-dev pkgconf make gcc perl && rustup component add clippy rustfmt && cargo fetch"
}
